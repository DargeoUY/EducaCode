services:
  web:
    build: ./backend
    ports:
      - "80:80"
    env_file:
      - .env
    depends_on:
      mariadb:
        condition: service_healthy
    volumes:
      - ./static:/data/static:ro
      - ./Diseño_Web:/data/static/Diseño_Web:ro
      - ./Algoritmia:/data/static/Algoritmia:ro
      - ./Algoritmo:/data/static/Algoritmo:ro
      - ./Python:/data/static/Python:ro
      - ./pseudocodigo:/data/static/pseudocodigo:ro
      - ./lti-test:/data/static/lti-test:ro
      - ./style:/data/static/style:ro
      - ./ico-img:/data/static/ico-img:ro
      - ./ide:/data/static/ide:ro
      - ./index.html:/data/static/index.html:ro
    networks:
      - app_net

  mariadb:
    image: mariadb:11
    ports:
      - "3306:3306"
    env_file:
      - .env
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root_pass_2026}
      MARIADB_DATABASE: ${MARIADB_DATABASE:-materiales1}
      MARIADB_USER: ${MARIADB_USER:-app_user}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD:-app_pass_2026}
    volumes:
      - ./db/data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app_net

networks:
  app_net:
    driver: bridge
